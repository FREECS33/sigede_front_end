import 'package:flutter/material.dart';

class CustomListInstitution extends StatelessWidget {
const CustomListInstitution({ super.key });

  @override
  Widget build(BuildContext context){
    return Container();
  }
}